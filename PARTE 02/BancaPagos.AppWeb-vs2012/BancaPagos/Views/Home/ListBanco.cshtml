@model IEnumerable<BancaPagos.BE.Banco>

@{
    ViewBag.Title = "ListBanco";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Nuevo", "CreateBanco")
</p>
<table>
    <tr>
        <th>
           id @*@Html.DisplayNameFor(model => model.id_banca)*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaRegistro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_banca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaRegistro)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditBanco", new { id=item.id_banca}) |            
           @* @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
        </td>
    </tr>
}

</table>
